-- Schema reference (Hibernate auto-creates tables on startup)
-- Main tables: users, jobs, internships, applications, resumes, events,
-- notifications, interviews, courses, course_progress, saved_jobs, contest_registrations

USE nina_db;
